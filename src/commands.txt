git clone https://github.com/kdua13/git_commands.git

#commands to commit a change
git add .
git commit - m "message text"
git push

#commands to reset to a previous commit id
git reset --hard <commit id>
git push --force